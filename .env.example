# Tripvar Environment Configuration Example
# Copy this file to .env and adjust values as needed

# =============================================================================
# DOCKER COMPOSE CONFIGURATION
# =============================================================================

# Build target for server (development or production)
BUILD_TARGET=development

# Environment suffix for .env files (e.g., .env.production)
ENV_SUFFIX=

# Volume mount configuration (set to empty string to disable volume mounting)
VOLUME_MOUNT=./tripvar-server:/usr/src/app

# Node environment
NODE_ENV=development

# Client port
CLIENT_PORT=5173

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Global log level (error, warn, info, debug)
# For development: use 'warn' to reduce noise
# For production: use 'info' for normal operation
LOG_LEVEL=warn

# MongoDB command (add --quiet for less verbose output)
MONGO_COMMAND=mongod --quiet --logpath /dev/null

# Redis command (add --loglevel warning for less verbose output)
REDIS_COMMAND=redis-server --appendonly yes --loglevel warning

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# MongoDB root credentials
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password

# MongoDB connection string (server will use these credentials)
# Format: mongodb://username:password@host:port/database?authSource=database
MONGODB_URI=mongodb://tripvar_user:tripvar_password123@mongodb:27017/tripvar?authSource=tripvar

# =============================================================================
# API CONFIGURATION
# =============================================================================

# API URL for client
VITE_API_URL=http://localhost:8000

# =============================================================================
# DEVELOPMENT TIPS
# =============================================================================

# To see more logs during development, set:
# LOG_LEVEL=info

# To see all logs including debug information:
# LOG_LEVEL=debug

# To see only errors and warnings (recommended for daily development):
# LOG_LEVEL=warn

# To see only errors:
# LOG_LEVEL=error